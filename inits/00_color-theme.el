;;
;;; color themeの設定
;;

(el-get-bundle monokai-theme

  ;; tango-darkテーマ(デフォルト)
  (load-theme 'tango-dark t)
  
  ;; monokaiテーマ
  ;; (load-theme 'monokai t)
  
  
  ;; カーソルの色を変更する
  (set-cursor-color "yellow")
  
  )
