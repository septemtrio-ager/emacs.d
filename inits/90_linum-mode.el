;;; 行番号を表示する

;; c-modeで利用する
(add-hook 'c-mode-hook 'linum-mode)

;; c++-modeで利用する
(add-hook 'c++-mode-hook 'linum-mode)

;; java-modeで利用する
(add-hook 'java-mode-hook 'linum-mode)

;; python-modeで利用する
(add-hook 'python-mode-hook 'linum-mode)

;; r-modeで利用する
(add-hook 'R-mode-hook 'linum-mode)

;; emacs-lisp-modeで利用する
(add-hook 'emacs-lisp-mode-hook 'linum-mode)

;; web-modeで利用する
(add-hook 'web-mode-hook 'linum-mode)

;; css-modeで利用する
(add-hook 'css-mode-hook 'linum-mode)

;; Github Flavored Markdown modeで利用する
(add-hook 'gfm-mode-hook 'linum-mode)

;; arduino-modeで利用する
(add-hook 'arduino-mode-hook 'linum-mode)

;; shell-script-modeで利用する
(add-hook 'sh-mode-hook 'linum-mode)

;; Markdown-modeで利用する
(add-hook 'markdown-mode-hook 'linum-mode)

;; octave-modeで利用する
(add-hook 'octave-mode-hook 'linum-mode)
